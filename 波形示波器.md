# 波形示波器任务计划

- [x] 1. 分析现有代码
  - [x] 检查 `serialportplot(h/cpp)` 和 UI 文件，了解串口接收逻辑和目前的波形显示机制（QCustomPlot）。
- [ ] 2. 修改波形数据解析逻辑
  - [ ] 在串口收到数据后，按字符串解析提取波形数据。
  - [ ] 匹配以 `$` 开头，以 `;` 结尾的整行数据。
  - [ ] 提取随后的以空格分隔的数据块。
  - [ ] 将提取的数据分配给不同的曲线通道（根据空格划分为多个通道）。如果通道数不足则动态创建并分配新颜色。
- [ ] 3. 完善波形显示和通道图例 (Legend)
  - [ ] 在 QCustomPlot 中启用 Legend (图例) 显示。
  - [ ] 设置各个图例项可见，让用户识别出颜色对应的通道。
- [ ] 4. 增加控制界面选项选项
  - [ ] 增加复选框：“隐藏收发区” (第一个选项按钮，用于隐藏数据接收和发送框以扩大波形显示区域)。
  - [ ] 增加复选框：“不显示原始数据” (第二个选项按钮，用于当波形数据量大时，不在接收区的文本框中显示接收的原始数据)。
  - [ ] 实现这两个复选框对应的槽函数或逻辑控制。
- [ ] 5. 测试验证
  - [ ] 编译通过。
  - [ ] 发送测试数据能正确触发新波形、各种通道色更新、UI 控制响应等。
