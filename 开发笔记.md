[toc]

# 1、项目树

# 2、界面主题开发

# 3、GCC编译和调试

# 4、Qt界面分栏

# 5 、开发代码编辑器

## **5.1、移植代码编辑器第三方库QScintilla**

以下是在 **Windows 系统**下安装和编译 **QScintilla** 的详细步骤指南：

------

### **5.1.1. 系统要求**

- **操作系统**: Windows 10/11 (64位推荐)
- 编译工具
  - **Visual Studio 2019/2022**（MSVC编译器）或 **MinGW**
  - **Qt 5.15+** 或 **Qt 6.x**（需与编译器匹配）
- **其他工具**: CMake（可选）、Git（可选）

------

### **5.1.2. 安装必要软件**

#### **5.1.2.1 安装 Qt**

1. 下载 **Qt 在线安装程序**：https://www.qt.io/download
2. 运行安装程序，选择以下组件：
   - **Qt 版本**: 如 `Qt 5.15.2` 或 `Qt 6.5.0`
   - **编译器**: 根据需求选择 `MSVC 2019` 或 `MinGW`
   - **Qt Creator**: 勾选以安装 IDE
3. 完成安装后，确保 `qmake` 已添加到系统环境变量（或通过 `Qt命令行` 操作）。

#### **5.1.2.2 安装 Visual Studio (MSVC用户)**

- 若使用 MSVC 编译器，安装 **Visual Studio 2019/2022**，并勾选 **“C++桌面开发”** 组件。

------

### **5.1.3. 下载 QScintilla 源码**

1. 访问 **QScintilla 官方页面**：https://www.riverbankcomputing.com/software/qscintilla/download
2. 下载最新源码包（如 `QScintilla_src-2.11.6.zip`）。***<u>这里可以下载最新版</u>***
3. 解压到本地目录，例如 `D:\Toolchains\QScintilla-2.11.6\QScintilla-2.11.6`。

------

### **5.1.4. 编译 QScintilla**

#### **5.1.4.1 编译 Qt5/Qt6 库**

1. 打开 

   Qt 命令行（根据编译器选择）：

   - 例如：`Qt 5.15.2 MSVC2019 64-bit` 或 `Qt 6.5.0 MinGW 64-bit`。

2. 进入源码目录的 Qt4Qt5子目录：

   ```bash
   cd D:\Toolchains\QScintilla-2.11.6\QScintilla-2.11.6\Qt4Qt5
   ```

3. 生成 Makefile：

   ```bash
   qmake qscintilla.pro
   ```

4. 编译代码：

   - MSVC 用户

     ```bash
     nmake
     nmake install
     ```

   - MinGW 用户

     ```bash
     mingw32-make
     mingw32-make install
     ```

5. 成功编译后，生成的库文件（`.dll` 和 `.lib`）将自动安装到 Qt 目录的 `plugins\designer` 和 `bin` 文件夹。

#### **5.1.4.2 编译设计器插件（可选）**

1. 进入 designer子目录：

   ```bash
   cd D:\Dev\QScintilla\designer
   ```

2. 生成 Makefile：

   ```bash
   qmake designer.pro
   ```

3. 编译并安装：

   - MSVC

     ```bash
     nmake
     nmake install
     ```

   - MinGW

     ```bash
     mingw32-make
     mingw32-make install
     ```

4. 插件文件（`qscintillaplugin.dll`）将出现在 `Qt\plugins\designer` 目录。

------

### **5.1.5. 测试 QScintilla**

1. 在 **Qt Creator** 中新建一个 Qt Widgets 项目。

2. 在项目文件`（.pro）`中添加 QScintilla 依赖：

   ```pro
   LIBS += -L"D:/Soft/Qt/5.15.2/mingw81_64/lib" -lqscintilla2_qt5
   INCLUDEPATH += "D:/Soft/Qt/5.15.2/mingw81_64/include/Qsci"
   ```

3. 在代码中引入头文件并使用 `QsciScintilla` 组件：

   ```cpp
   #include <Qsci/qsciscintilla.h>
   QsciScintilla *editor = new QsciScintilla(this);
   ```

------

### **5.1.6. 常见问题**

#### **5.1.6.1 生成的库文件（.dll 和 .lib）没有安装到 Qt 目录的 plugins\designer 和 bin 文件夹**

QScintilla 的默认安装路径可能未正确指向你的 Qt 目录。可以通过以下步骤手动指定路径：

1. 在 `Qt4Qt5` 目录中，编辑 `qscintilla.pro` 文件：

   ```pro
   # 添加以下两行（替换为你的 Qt 安装路径）
   win32 {
       DESTDIR = C:/Qt/5.15.2/msvc2019_64/bin      # DLL 输出目录
       target.path = C:/Qt/5.15.2/msvc2019_64/lib  # LIB 输出目录
   }
   ```

   > 注意：路径需使用 **正斜杠** `/`，且路径需与你的实际 Qt 安装路径匹配。

2. 重新生成 Makefile 并编译：

   ```bash
   qmake qscintilla.pro
   nmake clean
   nmake
   nmake install
   ```

### **附：典型目录结构示例**

假设你的 Qt 安装在 `C:\Qt\5.15.2\msvc2019_64`，编译后应有以下文件：

```markdown
C:\Qt\5.15.2\msvc2019_64
├── bin
│   └── qscintilla2_qt5.dll
├── lib
│   └── qscintilla2_qt5.lib
├── plugins
│   └── designer
│       └── qscintillaplugin.dll
└── include
    └── Qsci
        └── qsciscintilla.h
```

#### **5.1.6.2 QScintilla 和应用程序均使用 不同 MinGW 版本，会导致链接错误**

> ***错误信息是“QSC.exe - 无法找到入口”，具体提示是无法定位程序输入点_ZSt28_throw_bad_array_new_lengthv在动态链接库qscintilla2_qt5.dll上***

- 解决方案建议：

1. **统一编译器版本**：

   - 确保 QScintilla 和应用程序均使用 **相同 MinGW 版本**（如 8.1）编译。
   - 在 Qt Creator 中检查工具链设置：`工具 -> 选项 -> Kits -> 编译器/调试器`。

2. **修复 DLL 路径**：

   - 将 `qscintilla2_qt5.dll` 从 `lib` 目录移动到程序输出目录（如 `release` 或 `debug`）或系统 `PATH` 包含的路径。
   - 添加 Qt 的 `bin` 目录到系统环境变量：`D:\Soft\Qt\5.15.2\mingw81_64\bin`。

3. **重新编译 QScintilla**：

   - 使用以下命令强制生成兼容的 DLL：

     ```bash
   cd Qt4Qt5
     mingw32-make clean
   qmake qscintilla.pro
     mingw32-make install
   ```
   
4. **检查 C++ 运行时库**：

   - 确保 MinGW 的 `libstdc++-6.dll` 版本与 QScintilla 编译时使用的版本一致，必要时覆盖旧文件。

#### **5.1.6.3 设计器插件不显示**

- 确认插件文件已复制到 `Qt\plugins\designer`，重启 Qt Creator。

------



## 5.2、代码编辑器分栏

### **5.2.1 关闭分栏**

```cpp
// void CodeEditor::closeSplitView()
// {
//     if (!m_currentEditor || m_editors.size() <= 1) {
//         return;
//     }

//     // 获取当前编辑器的父分割器
//     QSplitter* parentSplitter = qobject_cast<QSplitter*>(m_currentEditor->parent());
//     if (!parentSplitter || parentSplitter == m_mainSplitter) {
//         return;
//     }

//     // 获取分割器中的另一个部件
//     QWidget* otherWidget = nullptr;
//     for (int i = 0; i < parentSplitter->count(); ++i) {
//         QWidget* widget = parentSplitter->widget(i);
//         if (widget != m_currentEditor) {
//             otherWidget = widget;
//             break;
//         }
//     }

//     if (!otherWidget) {
//         return;
//     }

//     // 获取父分割器的父部件
//     QWidget* grandParent =  qobject_cast<QWidget*>(parentSplitter->parent());
//     QSplitter* grandParentSplitter = qobject_cast<QSplitter*>(grandParent);

//     // 获取父分割器在其父分割器中的索引
//     int index = -1;
//     if (grandParentSplitter) {
//         index = grandParentSplitter->indexOf(parentSplitter);
//     } else if (grandParent == m_mainSplitter) {
//         index = m_mainSplitter->indexOf(parentSplitter);
//         grandParentSplitter = m_mainSplitter;
//     }

//     // 从父分割器中移除另一个部件
//     otherWidget->setParent(nullptr);

//     // 将另一个部件添加到父分割器的父部件中
//     if (index != -1 && grandParentSplitter) {
//         grandParentSplitter->insertWidget(index, otherWidget);
//     } else {
//         // 如果找不到父分割器的父部件，则添加到主分割器
//         m_mainSplitter->addWidget(otherWidget);
//     }

//     // 从编辑器列表中移除当前编辑器
//     m_editors.removeOne(m_currentEditor);

//     // 删除当前编辑器
//     delete m_currentEditor;

//     // 删除父分割器
//     delete parentSplitter;

//     // 更新当前编辑器
//     if (qobject_cast<QsciScintilla*>(otherWidget)) {
//         m_currentEditor = qobject_cast<QsciScintilla*>(otherWidget);
//     } else if (QSplitter* otherSplitter = qobject_cast<QSplitter*>(otherWidget)) {
//         // 如果另一个部件是分割器，则找到其中的第一个编辑器
//         m_currentEditor = findFirstEditor(otherSplitter);
//     } else {
//         m_currentEditor = m_editors.first();
//     }

//     // 将焦点设置到当前编辑器
//     if (m_currentEditor) {
//         m_currentEditor->setFocus();
//     }
// }
```



```cpp
// void CodeEditor::closeSplitView()
// {
//     if (!m_currentEditor || m_editors.size() <= 1) {
//         return;
//     }

//     // 获取当前编辑器的父分割器
//     QSplitter* parentSplitter = qobject_cast<QSplitter*>(m_currentEditor->parent());
//     if (!parentSplitter || parentSplitter == m_mainSplitter) {
//         return;
//     }

//     // 获取分割器中的另一个部件
//     QWidget* otherWidget = nullptr;
//     for (int i = 0; i < parentSplitter->count(); ++i) {
//         QWidget* widget = parentSplitter->widget(i);
//         if (widget != m_currentEditor) {
//             otherWidget = widget;
//             break;
//         }
//     }

//     if (!otherWidget) {
//         return;
//     }

//     // 获取父分割器的父部件 - 修复类型转换问题
//     QWidget* grandParent = qobject_cast<QWidget*>(parentSplitter->parent());
//     QSplitter* grandParentSplitter = nullptr;

//     // 检查父分割器的父部件是否是分割器或主分割器
//     if (grandParent == m_mainSplitter) {
//         grandParentSplitter = m_mainSplitter;
//     } else {
//         grandParentSplitter = qobject_cast<QSplitter*>(grandParent);
//     }

//     // 获取父分割器在其父分割器中的索引
//     int index = -1;
//     if (grandParentSplitter) {
//         index = grandParentSplitter->indexOf(parentSplitter);
//     }

//     // 从父分割器中移除另一个部件
//     otherWidget->setParent(nullptr);

//     // 将另一个部件添加到父分割器的父部件中
//     if (index != -1 && grandParentSplitter) {
//         grandParentSplitter->insertWidget(index, otherWidget);
//     } else {
//         // 如果找不到父分割器的父部件，则添加到主分割器
//         m_mainSplitter->addWidget(otherWidget);
//     }

//     // 从编辑器列表中移除当前编辑器
//     m_editors.removeOne(m_currentEditor);

//     // 删除当前编辑器
//     delete m_currentEditor;

//     // 删除父分割器
//     delete parentSplitter;

//     // 更新当前编辑器
//     if (qobject_cast<QsciScintilla*>(otherWidget)) {
//         m_currentEditor = qobject_cast<QsciScintilla*>(otherWidget);
//     } else if (QSplitter* otherSplitter = qobject_cast<QSplitter*>(otherWidget)) {
//         // 如果另一个部件是分割器，则找到其中的第一个编辑器
//         m_currentEditor = findFirstEditor(otherSplitter);
//     } else {
//         m_currentEditor = m_editors.first();
//     }

//     // 将焦点设置到当前编辑器
//     if (m_currentEditor) {
//         m_currentEditor->setFocus();
//     }
// }

```



```cpp
void CodeEditor::closeSplitView()
{
    if (!m_currentEditor || m_editors.size() <= 1) {
        return;
    }

    // 获取当前编辑器的父分割器
    QSplitter* parentSplitter = qobject_cast<QSplitter*>(m_currentEditor->parent());
    if (!parentSplitter || parentSplitter == m_mainSplitter) {
        return;
    }

    // 获取分割器中的另一个部件
    QWidget* otherWidget = nullptr;
    for (int i = 0; i < parentSplitter->count(); ++i) {
        QWidget* widget = parentSplitter->widget(i);
        if (widget != m_currentEditor) {
            otherWidget = widget;
            break;
        }
    }

    if (!otherWidget) {
        return;
    }

    // 从编辑器列表中移除当前编辑器
    m_editors.removeOne(m_currentEditor);

    // 保存当前编辑器的引用，以便后面删除
    QsciScintilla* editorToDelete = m_currentEditor;

    // 获取父分割器的父部件
    QWidget* grandParent = qobject_cast<QWidget*>(parentSplitter->parent());
    QSplitter* grandParentSplitter = nullptr;

    // 先更新当前编辑器引用，避免使用已删除的指针
    if (qobject_cast<QsciScintilla*>(otherWidget)) {
        m_currentEditor = qobject_cast<QsciScintilla*>(otherWidget);
    } else if (QSplitter* otherSplitter = qobject_cast<QSplitter*>(otherWidget)) {
        // 如果另一个部件是分割器，则找到其中的第一个编辑器
        m_currentEditor = findFirstEditor(otherSplitter);
    } else {
        // 如果没有找到合适的编辑器，使用列表中的第一个
        m_currentEditor = m_editors.isEmpty() ? nullptr : m_editors.first();
    }

    // 处理特殊情况：如果父部件是主分割器
    if (grandParent == m_mainSplitter) {
        // 从父分割器中移除另一个部件（先保存引用）
        otherWidget->setParent(nullptr);

        // 获取父分割器在主分割器中的索引
        int index = m_mainSplitter->indexOf(parentSplitter);

        // 将另一个部件添加到主分割器
        m_mainSplitter->insertWidget(index, otherWidget);
    }
    else {
        // 处理嵌套分割器的情况
        grandParentSplitter = qobject_cast<QSplitter*>(grandParent);
        if (!grandParentSplitter) {
            // 安全检查失败，恢复编辑器列表并返回
            m_editors.append(editorToDelete);
            return;
        }

        // 获取父分割器在祖父分割器中的索引
        int index = grandParentSplitter->indexOf(parentSplitter);

        // 从父分割器中移除另一个部件
        otherWidget->setParent(nullptr);

        // 将另一个部件添加到祖父分割器
        grandParentSplitter->insertWidget(index, otherWidget);
    }

    // 删除当前编辑器和父分割器
    delete editorToDelete;
    delete parentSplitter;

    // 将焦点设置到当前编辑器
    if (m_currentEditor) {
        m_currentEditor->setFocus();
    }
}

// 添加一个辅助方法来查找分割器中的第一个编辑器
QsciScintilla* CodeEditor::findFirstEditor(QSplitter* splitter)
{
    if (!splitter) {
        return nullptr;
    }
    
    for (int i = 0; i < splitter->count(); ++i) {
        QWidget* widget = splitter->widget(i);
        
        // 如果是编辑器，直接返回
        QsciScintilla* editor = qobject_cast<QsciScintilla*>(widget);
        if (editor) {
            return editor;
        }
        
        // 如果是分割器，递归查找
        QSplitter* childSplitter = qobject_cast<QSplitter*>(widget);
        if (childSplitter) {
            QsciScintilla* foundEditor = findFirstEditor(childSplitter);
            if (foundEditor) {
                return foundEditor;
            }
        }
    }
    
    return nullptr;
}
```





+ 下面这段关闭分栏的代码，只能实现关闭上一次的分栏，如：分栏了三次，则只会关闭第三次的分栏，再点击关闭分栏后不再关闭分栏

``` cpp
// void CodeEditor::closeSplitView()
// {
//     if (!m_currentEditor || m_editors.size() <= 1) {
//         return;
//     }

//     // 获取当前编辑器的父分割器
//     QSplitter* parentSplitter = qobject_cast<QSplitter*>(m_currentEditor->parent());
//     if (!parentSplitter || parentSplitter == m_mainSplitter) {
//         return;
//     }

//     // 获取分割器中的另一个部件
//     QWidget* otherWidget = nullptr;
//     for (int i = 0; i < parentSplitter->count(); ++i) {
//         QWidget* widget = parentSplitter->widget(i);
//         if (widget != m_currentEditor) {
//             otherWidget = widget;
//             break;
//         }
//     }

//     if (!otherWidget) {
//         return;
//     }

//     // 获取父分割器在主分割器中的索引
//     int index = m_mainSplitter->indexOf(parentSplitter);

//     // 从父分割器中移除另一个部件
//     otherWidget->setParent(nullptr);

//     // 将另一个部件添加到主分割器
//     m_mainSplitter->insertWidget(index, otherWidget);

//     // 从编辑器列表中移除当前编辑器
//     m_editors.removeOne(m_currentEditor);

//     // 删除当前编辑器
//     delete m_currentEditor;

//     // 删除父分割器
//     delete parentSplitter;

//     // 更新当前编辑器
//     if (qobject_cast<QsciScintilla*>(otherWidget)) {
//         m_currentEditor = qobject_cast<QsciScintilla*>(otherWidget);
//     } else {
//         m_currentEditor = m_editors.first();
//     }

//     // 将焦点设置到当前编辑器
//     m_currentEditor->setFocus();
// }
```

+ 这段代码关闭分栏会导致系统崩溃

```cpp
//这段代码关闭分栏会导致系统崩溃
// void CodeEditor::closeSplitView()
// {
//     if (!m_currentEditor || m_editors.size() <= 1) {
//         return;
//     }

//     // 获取当前编辑器的父分割器
//     QSplitter* parentSplitter = qobject_cast<QSplitter*>(m_currentEditor->parent());
//     if (!parentSplitter || parentSplitter == m_mainSplitter) {
//         return;
//     }

//     // 获取分割器中的另一个部件
//     QWidget* otherWidget = nullptr;
//     for (int i = 0; i < parentSplitter->count(); ++i) {
//         QWidget* widget = parentSplitter->widget(i);
//         if (widget != m_currentEditor) {
//             otherWidget = widget;
//             break;
//         }
//     }

//     if (!otherWidget) {
//         return;
//     }

//     // 获取父分割器的父部件
//     QWidget* grandParent = qobject_cast<QWidget*>(parentSplitter->parent());

//     // 处理特殊情况：如果父部件是主分割器
//     if (grandParent == m_mainSplitter) {
//         // 从父分割器中移除另一个部件
//         parentSplitter->insertWidget(parentSplitter->indexOf(m_currentEditor), nullptr);
//         otherWidget->setParent(nullptr);

//         // 获取父分割器在主分割器中的索引
//         int index = m_mainSplitter->indexOf(parentSplitter);

//         // 将另一个部件添加到主分割器
//         m_mainSplitter->insertWidget(index, otherWidget);

//         // 从编辑器列表中移除当前编辑器
//         m_editors.removeOne(m_currentEditor);

//         // 删除当前编辑器和父分割器
//         delete m_currentEditor;
//         delete parentSplitter;
//     }
//     else {
//         // 处理嵌套分割器的情况
//         QSplitter* grandParentSplitter = qobject_cast<QSplitter*>(grandParent);
//         if (!grandParentSplitter) {
//             return; // 安全检查
//         }

//         // 获取父分割器在祖父分割器中的索引
//         int index = grandParentSplitter->indexOf(parentSplitter);

//         // 从父分割器中移除另一个部件
//         parentSplitter->insertWidget(parentSplitter->indexOf(m_currentEditor), nullptr);
//         otherWidget->setParent(nullptr);

//         // 将另一个部件添加到祖父分割器
//         grandParentSplitter->insertWidget(index, otherWidget);

//         // 从编辑器列表中移除当前编辑器
//         m_editors.removeOne(m_currentEditor);

//         // 删除当前编辑器和父分割器
//         delete m_currentEditor;
//         delete parentSplitter;
//     }

//     // 更新当前编辑器
//     if (qobject_cast<QsciScintilla*>(otherWidget)) {
//         m_currentEditor = qobject_cast<QsciScintilla*>(otherWidget);
//     } else if (QSplitter* otherSplitter = qobject_cast<QSplitter*>(otherWidget)) {
//         // 如果另一个部件是分割器，则找到其中的第一个编辑器
//         m_currentEditor = findFirstEditor(otherSplitter);
//     } else {
//         m_currentEditor = m_editors.first();
//     }

//     // 将焦点设置到当前编辑器
//     if (m_currentEditor) {
//         m_currentEditor->setFocus();
//     }
// }
```

## **5.3、代码编辑器语法高亮、代码自动补全、代码折叠**







# 6、数据库开发



# 7、串口示波器

# 8、网络开发



-----------------------

# *9 、问题及day Tips*



1. 编辑器分栏，没法实现对选中的编辑器进行分栏；
2. 编辑器分栏，每个编辑器需要添加工具栏（水平分栏、垂直分栏、删除分栏），只有一个编辑器时删除分栏按钮消失；
3. 编辑器暂时无法实现函数、变量名等显示不同颜色；
4. 编辑器暂时无法实现不同花括号对显示不同颜色
5. STM32工程无法实现编译
6. 由于m_editor是在mainwindow中setupeditor中内部实例化的，导致有些函数在调用setupeditor之前就对m_editor操作，导致系统崩溃，需要全面解决。
