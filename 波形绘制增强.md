# 实现波形绘制增强功能

此更新旨在根据要求实现以下与波形显示相关的功能：
1. 增加波形绘制停止按钮
2. 未接收到数据时，波形绘制面板中不显示通道导航图（Legend）
3. 增加曲线图复制、导出为PNG等主流格式的功能

## Proposed Changes

### STM32IDE
针对 [SerialSession](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#21-45) 中的波形显示组件进行改造。

#### [MODIFY] serialportplot.h
- 在 [SerialSession](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#21-45) 类声明中：
  - 添加成员变量 `QPushButton *m_btnStopWaveform;` 
  - 添加私有槽函数 `void onChartContextMenu(const QPoint &pos);` 用于响应波形面板上的右键菜单请求。

#### [MODIFY] serialportplot.cpp
- **1. 界面与控制器更新 ([setupUi](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#51-609))：**
  - 实例化 `m_btnStopWaveform` 按钮，设置为带选中状态的 Checkable 按钮（"停止绘制"），加入到 `m_dockSettings` 的 `m_settingsLayout` 布局中。
  - 为 `m_customPlot` 启用自定义右键菜单策略：`m_customPlot->setContextMenuPolicy(Qt::CustomContextMenu);`。
- **2. 未接收数据时不显示通道图 ([setupChart](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#610-640) & [updateWaveform](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#962-1042))：**
  - 在 [setupChart](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#610-640) 中，不强制添加默认的 Graph(0) 和默认 `CH1`，同时初始状态将 Legend 隐藏：`m_customPlot->legend->setVisible(false);`。
  - 在 [updateWaveform](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#962-1042) 中，如果要添加新数据时，若有至少一条通道且当前图例被隐藏，则重新显示：`m_customPlot->legend->setVisible(true);`。同时，如果 `m_btnStopWaveform->isChecked()`，则直接返回（或者在接收数据时就不拼接给解析Buffer），实现暂停绘制的效果。
- **3. 右键菜单实现 (`onChartContextMenu`)：**
  - 连接 `customContextMenuRequested` 信号到 `onChartContextMenu`。
  - 弹出一个 `QMenu`，提供三个 QAction 项：`复制到剪贴板`、`导出为 PNG`、`导出为 JPG`。
  - 实现逻辑：
    - `复制到剪贴板`: `QApplication::clipboard()->setPixmap(m_customPlot->toPixmap());`
    - `导出为 PNG`: 弹出 `QFileDialog::getSaveFileName` 对话框，获取文件名后调用 `m_customPlot->savePng()` 保存。
    - `导出为 JPG`: 类似于PNG导出功能，调用 `saveJpg()`。
- **4. 重置按钮修复 ([setupConnections](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#641-769))：**
  - 修改 `m_btnResetChart` 的点击事件逻辑。之前的代码仅清除了 `graph(0)` 的数据，需要修改为 `m_customPlot->clearGraphs();`，重置 `m_xValue`，并且隐藏通道导航图。

## Verification Plan

### Automated Tests
- 不涉及自动化测试用例，主要为 GUI 与交互改变。

### Manual Verification
1. 打开串口接收带有图表格式 (`$ 1.5 2.0 ;`) 的数据。
2. 观察通道1和通道2的产生：确认没有数据前，图表右上方没有“通道导航图”(Legend)，有数据后才出现。
3. 点击“停止绘制”按钮，继续接收串口数据，波形停止更新；再次点击，波形继续更新新接收到的数据。
4. 在波形绘制区域右键，选择“复制到剪贴板”，然后粘贴到 Word 或者画图工具中，确认粘贴是否为整个波形截图。
5. 右键选择“导出为 PNG”，在文件对话框选择保存路径且检查是否能够成功打开对应的 PNG 文件。
6. 点击“重置”按钮，确认波形从头开始，通道导航被清空/隐藏。
