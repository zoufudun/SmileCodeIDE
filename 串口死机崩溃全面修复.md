# 串口死机/崩溃全面修复

当第一个串口开启自动发送的情况下，通过分屏或新建标签页打开第二个串口时，程序出现崩溃/死机。代码分析发现以下4个根本性问题。

## 发现的问题（按危险程度排序）

> [!CAUTION]
> **Bug 1（最严重 — 必然崩溃）：`static` 数据缓冲区跨实例共享**
>
> 位于 [updateWaveform()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1476-1651) 第 1482 行：
> ```cpp
> static QString buffer;  // ← 被所有 SerialSession 实例共享！
> ```
> `static` 局部变量在整个进程中只有一份。当两个串口同时接收数据时，两个 [SerialSession](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#26-56) 实例会交替向同一个 `buffer` 写入，导致数据损坏，并极大可能触发越界访问或无限递归而崩溃。**这是主要崩溃原因。**

> [!WARNING]
> **Bug 2：[openClosePort()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1308-1372) 中三重重复关闭**
>
> 第 1310–1312 行：
> ```cpp
> m_serial->close();
> m_serial->close();  // ← 多余
> m_serial->close();  // ← 多余
> ```
> 重复关闭一个已关闭的 `QSerialPort` 可能导致底层状态机异常，特别是在并发使用多个会话时。

> [!WARNING]
> **Bug 3：[hideEvent](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1905-1913) 只停了 `autoSendTimer`，遗漏了 `multiLoopTimer`**
>
> 第 1905–1912 行的 [hideEvent()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1905-1913) 中：
> ```cpp
> if (m_autoSendTimer->isActive()) {
>     m_autoSendTimer->stop();
>     m_chkAutoSend->setChecked(false);
> }
> // ← multiLoopTimer 没有被停止！
> ```
> 当用户切换到另一个分屏/标签页，当前 session 被隐藏时，多条发送循环计时器依然在后台运行，持续调用 [onMultiSendLoop()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#2169-2219)，并对一个不再可见的 session 进行 `m_serial->write()` 操作，可能导致后续崩溃。

> [!NOTE]
> **Bug 4（潜在问题）：[onPortError](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1373-1379) 在信号处理中弹出 `QMessageBox`**
>
> 第 1373–1378 行：
> ```cpp
> void SerialSession::onPortError(...) {
>     if (error == QSerialPort::ResourceError) {
>         QMessageBox::critical(this, "严重错误", "串口连接中断！");  // ← 阻塞！
>         openClosePort(); // ← 递归调用风险
>     }
> }
> ```
> 在串口错误信号的处理槽中直接弹出模态对话框并调用 [openClosePort()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1308-1372)，可能导致事件循环重入（re-entrancy），特别是在多会话并发时。应使用 `QMetaObject::invokeMethod` 推迟到下一个事件循环迭代执行。

## 修改方案

### serialportplot.cpp

#### [MODIFY] [serialportplot.cpp](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp)

**Fix 1**：将 [updateWaveform()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1476-1651) 中的 `static QString buffer` → 改为 [SerialSession](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#26-56) 的**成员变量** `m_rxBuffer`（需同步在 [.h](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.h) 中添加）。

**Fix 2**：删除 [openClosePort()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1308-1372) 中两个多余的 `m_serial->close()` 调用。

**Fix 3**：在 [hideEvent()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1905-1913) 中增加对 `m_multiLoopTimer` 的停止逻辑。

**Fix 4**：将 [onPortError()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1373-1379) 中的 `QMessageBox::critical` + [openClosePort()](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#1308-1372) 改为使用 `QTimer::singleShot(0, ...)` 推迟到下一事件循环迭代执行，避免重入。

#### [MODIFY] [serialportplot.h](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.h)

在 [SerialSession](file:///m:/TOPFIRE/SmileCodeIDE/STM32IDE/serialportplot.cpp#26-56) 类的私有成员中添加 `QString m_rxBuffer;`。

## 验证计划

### 手动验证步骤

1. 编译项目（`qmake + nmake` 或 Qt Creator 构建）
2. 启动程序，打开第一个串口，勾选**自动发送**并启动
3. 点击**水平分屏**按钮，在新面板中打开第二个串口
4. 观察程序是否仍然正常运行（**不崩溃、不卡死**）
5. 同时在两个面板中启用自动发送，持续观察 10 秒
6. 切换标签页，验证被隐藏的 session 的多条发送是否已暂停
7. 关闭一个分屏，验证程序是否正常关闭
